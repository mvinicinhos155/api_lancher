version: '3.9'

services:
   postgres:
      image: postgres:16
      container_name: api_lancher
      environment:
        POSTGRES_USER: root
        POSTGRES_PASSWORD: 1234
        POSTGRES_DB: api_luncher
      ports:
        - "5432:5432"